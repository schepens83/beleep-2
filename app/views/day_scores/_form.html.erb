<%= form_with(model: day_score, local: true) do |form| %>
  <% if day_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(day_score.errors.count, "error") %> prohibited this day_score from being saved:</h2>

      <ul>
      <% day_score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :day %>
    <%= form.date_select :day, id: :day_score_day %>
  </div>

  <div class="field">
    <%= form.label :tension %>
    <%= form.text_field :tension, id: :day_score_tension %>
  </div>

  <div class="field">
    <%= form.label :sleep %>
    <%= form.text_field :sleep, id: :day_score_sleep %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
